<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
    </style>

    <style name="AppTheme.NestedScroll" parent="AppTheme">
        <item name="nestedScroll">true</item>
        <item name="windowToolbar">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <!--<style name="SpecialButton" parent="Widget.AppCompat.Button.Colored">-->
        <!--<item name="colorButtonNormal">@android:color/holo_red_light</item>-->
        <!--<item name="android:textColorPrimary">#fff</item>-->
    <!--</style>-->

    <style name="SpecialButton" parent="Theme.AppCompat.Light">
        <item name="colorButtonNormal">@android:color/holo_blue_light</item>
        <item name="android:textColorPrimary">#fff</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
    </style>

    <style name="MaterialAnimations.Button" parent="Base.Widget.AppCompat.Button">
        <item name="colorButtonNormal">@android:color/holo_blue_light</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="MaterialAnimations.Text.Body" parent="Base.TextAppearance.AppCompat.Medium">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">8dp</item>
        <item name="android:padding">8dp</item>
    </style>


    <style name="MyDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!--buttons color-->
        <item name="colorAccent">@android:color/white</item>
        <!--title and message color-->
        <item name="android:textColorPrimary">@android:color/black</item>
        <!--dialog background-->
        <item name="android:windowBackground">@drawable/background_dialog</item>
    </style>


    <style name="MyDialogTheme2" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!--item RadioButton or CheckBox color-->
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorControlActivated">@android:color/black</item>
        <!--item text color-->
        <item name="textColorAlertDialogListItem">@android:color/white</item>
        <!--buttons color-->
        <item name="colorAccent">@android:color/white</item>
        <!--title and message color-->
        <item name="android:textColorPrimary">@android:color/white</item>
        <!--dialog background-->
        <item name="android:windowBackground">@drawable/background_dialog</item>
        <!--单选和多选的图片 -->
      <!--  <item name="android:listChoiceIndicatorMultiple">@drawable/selector_checkbox</item>
        <item name="android:listChoiceIndicatorSingle">@drawable/selector_radio_button</item>-->
    </style>

    <style name="myCheckBox" parent="AlertDialog.AppCompat.Light">
        <item name="colorControlNormal">@android:color/holo_blue_light</item>
        <item name="colorControlActivated">@android:color/black</item>
    </style>

    <style name="MySwitch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@android:color/holo_blue_bright</item>

        <!-- inactive thumb color 圆点的颜色-->
        <item name="colorSwitchThumbNormal">@android:color/white</item>

        <!-- inactive track color (30% transparency) 轨道的颜色-->
        <item name="android:colorForeground">@color/blue</item>
    </style>

    <declare-styleable name="SemicircleView">
        <attr name="radius" format="dimension" />
        <attr name="circleRadius" format="dimension" />
        <attr name="strokeWidth" format="dimension" />
        <attr name="bgArcStartColor" format="color" />
        <attr name="bgArcEndColor" format="color" />
        <attr name="tipTxtColor" format="color" />
        <attr name="percentTxtColor" format="color" />
        <attr name="tipsTextSize" format="dimension" />
        <attr name="percentTextSize" format="dimension" />
    </declare-styleable>

</resources>
